apiVersion: v1
kind: Secret
metadata:
  name: {{ kafka_secret_name }}
  namespace: {{ namespace }}
type: Opaque
stringData:
  KAFKA_HOST: {{ kafka_host }}
  KAFKA_PORT: "{{ kafka_port }}"
  KAFKA_PORT_2: "{{ kafka_port_2 }}"
  OKAPI_URL: {{ okapi_url }}
