This role adds permissions for all modules enabled by the tenant for the tenant admin user.  
It assumes that modules have been enabled for the tenant and that the tenant admin user 
(as well as tenant admin user uuid) has been created. 
